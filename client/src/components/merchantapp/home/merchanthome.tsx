import React from 'react';
import { Text } from '@chakra-ui/core';
import { DefaultPageProps } from 'types/';

export const MerchantHome: React.FC<DefaultPageProps> = (): JSX.Element => {
  return <Text>Welcome to the merchant home page</Text>;
};
