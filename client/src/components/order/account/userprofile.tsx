import * as React from 'react';
import { Box } from '@chakra-ui/react';

export const UserProfile = (): JSX.Element => {
  /**
   * Put the user profile stuff here!
   */
  return <Box>Users profile!</Box>;
};
