import React from 'react';
import { Button, Text } from '@chakra-ui/core';
import { DefaultPageProps } from 'types';

export const OrderHome: React.FC<DefaultPageProps> = () => {
  return <Text>Where do you want to order from?</Text>;
};
